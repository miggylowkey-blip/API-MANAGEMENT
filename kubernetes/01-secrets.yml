# This file is a template. For production use, create the secret from .env instead of committing real values.
apiVersion: v1
kind: Secret
metadata:
  name: api-managementz-secret
  namespace: api-managementz
type: Opaque
stringData:
  AUTH_SECRET: REPLACE_WITH_AUTH_SECRET
  DB_USER: REPLACE_WITH_DB_USER
  DB_PASSWORD: REPLACE_WITH_DB_PASSWORD
  DB_NAME: REPLACE_WITH_DB_NAME
  DB_SSLMODE: REPLACE_WITH_DB_SSLMODE
