# App
PORT=8080
AUTH_SECRET=change_me
RATE_LIMIT_RPS=5
RATE_LIMIT_BURST=10
REDIS_URL=redis://localhost:6379

# DB (pick ONE style)
# Style A: DATABASE_URL (recommended)
DATABASE_URL=postgres://api_managementz:api_managementz@localhost:5432/api_managementz?sslmode=disable

# Style B: parts (used if DATABASE_URL is empty)
DB_HOST=localhost
DB_USER=api_managementz
DB_PASSWORD=api_managementz
DB_NAME=api_managementz
DB_SSLMODE=disable
